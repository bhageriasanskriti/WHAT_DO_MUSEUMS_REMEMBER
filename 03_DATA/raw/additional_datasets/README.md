# Add remaining datasets here

Place each new dataset in its own folder and preserve its original internal folder structure. Do not flatten or edit the source files. After adding a dataset, update `03_DATA/DATASET_REGISTER.md`, `03_DATA/DATA_DICTIONARY.md`, and the adapter/config before rebuilding processed data.
