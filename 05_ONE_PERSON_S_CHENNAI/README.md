# Focused Archive

The package template requested a folder named `05_ONE_PERSON_S_CHENNAI`.

The supplied focused archive in this project is **Teenie Harris**, held by Carnegie Museum of Art, and the story specification is therefore about Teenie Harris rather than Chennai.

Do not relabel Teenie Harris as a Chennai archive. If a Chennai-specific dataset is supplied later, add it as a separate dataset under `03_DATA/raw/additional_datasets/` and update the narrative only if the evidence supports it.
